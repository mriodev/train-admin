import BaseLayout from "../utils/BaseLayout";
import TrainShedule from "../components/TrainShedule";


const SheduleList = () => {
    return (
        <BaseLayout children={<TrainShedule />} />
    );
}
export default SheduleList

